{
  "name": "resourceallocation-suite",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "PulseDeck launcher + CapacityLens + ScorePulse + Flowboard",
  "scripts": {
    "install:suite": "npm install --prefix CapacityLens/app && npm run install:all --prefix CapacityLens/app && npm install --prefix ScorePulse/client && npm install --prefix PulseDeck && npm install --prefix Flowboard",
    "dev:suite": "npx concurrently -n deck,lens,pulse,flow -c magenta,teal,gold,blue \"npm run dev --prefix PulseDeck\" \"npm run dev --prefix CapacityLens/app\" \"npm run dev --prefix ScorePulse/client\" \"npm run dev --prefix Flowboard\"",
    "dev:pulsedeck": "npm run dev --prefix PulseDeck",
    "dev:capacitylens": "npm run dev --prefix CapacityLens/app",
    "dev:scorepulse": "npm run dev --prefix ScorePulse/client",
    "dev:flowboard": "npm run dev --prefix Flowboard",
    "build:pages": "node scripts/build-pages.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
